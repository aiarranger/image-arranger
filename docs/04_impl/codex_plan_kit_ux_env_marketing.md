---
role: plan
depends_on:
  - docs/04_impl/implementation_plan.md
  - docs/request-spec.md
  - docs/marketing/video-shotlist.md
---
# 実装計画 v2 — Create kit UX改善・環境固定・マーケティング（Codex 引き継ぎ）

## 概要

本計画は **Codex（コーディングエージェント）が実装・テストを担当する** 前提の引き継ぎドキュメント。
2026-06-12〜13 のアップリフト（LP刷新・モーションシステム・サンプルデッキv2・デモエージェント・
ライブポーリング・品質ゲート・Cmd+Kパレット・初回ツアー）の **後続フェーズ** を定義する。

製品哲学は不変: **依存ゼロ・ローカルファースト・生成は外部サービスに委ねる（本体は絶対に生成APIを呼ばない）**。

### 外部サービス・リファレンスの適用可否

`ref-google-cloud` / `ref-supabase` / `ref-vercel-deploy` / `ref-gemini-api` は **本プロジェクトに適用しない**
（依存ゼロのローカル Node ツールであり、DB・OAuth・サーバーレスデプロイが存在しないため。
詳細は `implementation_plan.md` の同名セクション参照）。外部接点は npm / GitHub（Pages・Release）と、
Phase 4 のマーケティング投稿先（X / note / dev.to 等 — すべて 👤 が投稿）のみ。

### 担当凡例

| 記号 | 担当者 | 説明 |
|------|--------|------|
| 👤 | 人間 | 投稿・アカウント操作・最終視覚確認・録画 |
| 🤖 | Codex | コーディング・テスト・ドキュメント生成 |
| 🤖S | Codex のサブエージェント | **新規コンテキストで起動する検証専用エージェント**（実装者と分離） |
| 👤+🤖 | 共同 | 🤖が実行し👤が確認・承認 |

### 品質担保の大原則（全フェーズ共通・必読）

1. **実装と検証の分離**: 各タスク完了時、実装した Codex 自身の確認に加えて、
   **コンテキストを持たないサブエージェント（🤖S）に検証を依頼する**。サブエージェントには
   「やったこと」を教えず、**受け入れ条件のみ** を渡して合否判定させる（実装者バイアスの排除）。
2. **毎タスク必須コマンド**（1つでも落ちたらタスク未完了）:
   ```bash
   npm run check                       # 構文チェック（server/app/gallery/scripts）
   npm test                            # 2026-06-13 時点 37 件全パス + 自分が追加した分
   node server.mjs --workspace ./workspace/demo --init sample --doctor   # 公開健全性
   ```
3. **i18n パリティ**: UI 文言を追加したら必ず `t()` 経由とし、EN/JA **両方の I18N マップ** に
   キーを追加。検証: 両マップのキー数・キー集合の一致を node ワンライナーで確認する
   （app.js から I18N の ja/en オブジェクトを抽出して `Object.keys` を比較。差分ゼロが合格）。
4. **CSP/オフライン**: 外部リソース読み込み・インラインscript属性の追加は禁止
   （`default-src 'self'`）。検証: 起動した各ページの HTML に `http(s)://` のリソース参照が
   ないことを grep。
5. **契約の不変**: `image-arranger-request.v1`（docs/request-spec.md）と deck スキーマは
   **後方互換を壊さない**。フィールド追加は可、変更・削除は不可。
6. **既存機能のデグレ禁止**: renderT()（View Transitions）・livePoll（5秒ポーリング）・
   Undoトースト・品質ゲート・Cmd+Kパレット・初回ツアーを壊さないこと。タッチした画面は
   reduced-motion / キーボード操作 / Escape を必ず再確認。
7. **作業ログ**: 各フェーズの実装ログ・スクリーンショットを `agent-logs/<日付>-codex-<phase>/`
   に保存（リポジトリ規約。`agent-logs/` は gitignore 済み）。

### フェーズ一覧

| Phase | 名称 | 主な担当 | 目標 | 状態 |
|-------|------|---------|------|------|
| 0 | 環境の固定（codex / claude code 両対応起動） | 🤖 | どちらのエージェントでも1コマンドで起動・処理できる | ⏳ |
| 1 | Create kit UX改善（ルートA/B の分かりにくさ解消） | 🤖 | 初心者がヘルプなしで両ルートを完走できる | ⏳ |
| 2 | カラーパレット統合（シート品質向上） | 🤖 | パレットを正としてシート生成に組み込む | ⏳ |
| 3 | 初心者テスト＆磨き込み | 🤖S+👤 | コンテキストゼロの初見ユーザーが15分で WOW に到達 | ⏳ |
| 4 | マーケティング実施 | 🤖+👤 | X / note / 海外ブログへの投稿（👤が実施） | ⏳ |

---

## Phase 0: 環境の固定 ⏳

**目標**: このシステムを **Codex / Claude Code のどちらからでも** 同じ手順で起動・処理・テスト
できる状態に固定する。エージェントごとに手順が分岐しない「正準コマンド」を1セットに定める。

**背景（現状の実測値）**:
- 起動: `npm start` = `node server.mjs --workspace ./workspace/demo --init sample --port 4217`
- デモ処理: `npm run demo-agent`（`scripts/demo-agent.mjs`、対象 4217）
- 実処理: `node scripts/process-queue.mjs --server http://127.0.0.1:4217`（CDP 自動化 Chrome、専用プロファイル、port 9377）
- エージェント向け説明: `AGENTS.md`（Codex はこれを自動で読む）。**Claude Code 用の `CLAUDE.md` は存在しない** ← 今回固定する
- Node >= 20（package.json engines）

#### 実装ステップ

| # | タスク | 実施者 | 完了条件 | 状態 |
|---|--------|--------|---------|------|
| 0.1 | `CLAUDE.md` 新規作成: 正準コマンド（起動・テスト・処理・doctor）と「AGENTS.md を参照」の1ページ。AGENTS.md と内容が二重管理にならないよう、コマンド表は CLAUDE.md → AGENTS.md 参照の一方向にする | 🤖 | Claude Code がゼロ知識で `npm start`→`npm run demo-agent` に到達できる記載 | ⏳ |
| 0.2 | AGENTS.md 冒頭に「Quick bootstrap」節を追加: ①Node バージョン確認 ②`npm start` ③ブラウザで 127.0.0.1:4217 ④`npm run demo-agent`（キュー処理のお試し。**画像系リクエストのみ処理し、サンプル初期投入済みの動画リクエストはスキップする仕様** — その旨を1行注記）⑤`npm test` の5行 | 🤖 | コピペだけで起動完了する | ⏳ |
| 0.3 | 起動前フライトチェックを server.mjs 起動メッセージに統合: ポート使用中なら「`--port <別番号>` を指定」を **具体的コマンド付き** でエラー表示（現状の生エラーを置換） | 🤖 | 4217 占有状態で起動した際、次に打つべきコマンドがそのまま表示される | ⏳ |
| 0.4 | CI に「boot smoke」ジョブ追加: サーバー起動→`curl -sf http://127.0.0.1:4217/` 200→`npm run demo-agent -- --once` がエラー無く完走→kill | 🤖 | ci.yml で 3 OS不要・ubuntu のみ・Node 20/22/24 マトリクス維持 | ⏳ |
| 0.5 | 🤖S 検証: **新規コンテキストのサブエージェント** に「このリポジトリを clone した直後だと思って、リポジトリ内ドキュメントだけを頼りに起動し、**UI から画像系リクエスト（Image エントリ or Create kit のシート）を1件キュー投入してから `npm run demo-agent` で処理させ、結果が候補として登録されるところまで** 完走せよ」と指示。ソースコードを読まずに完走できるか判定。※サンプル初期投入の pending は **video** のため demo-agent では処理されない — これを検証手順の前提として明記しておく（誤って「処理されない＝バグ」と報告させない） | 🤖S | 完走 & 詰まった箇所のレポートが空 | ⏳ |

#### Phase 0 完了条件

- [ ] `CLAUDE.md` / `AGENTS.md` の正準コマンドが一致（重複記載なし・参照で解決）
- [ ] ポート衝突時のエラーメッセージが自己解決可能
- [ ] CI boot smoke がグリーン
- [ ] 🤖S のゼロ知識起動テスト合格（レポートを agent-logs に保存）

---

## Phase 1: Create kit UX改善 ⏳

**目標**: Create kit タブの「A. シートを作る」「B. パーツに分解」の操作の分かりにくさを解消する。

**現状の実装箇所（実測）**:
- レンダリング: `public/app.js` の kit タブ描画（`renderKit` 相当、〜1820行付近）
- ルートA: シート名 + テンプレ選択（`BUILTIN_SHEET_TEMPLATE`、app.js:1760）+ プロンプト textarea + 「キューに入れる」
- ルートB: キャラ名 + 追加要望 + 「解析を依頼」→ 完了後 `state.kitResults` →「パーツを選ぶ」→ チェックボックス → 取り込み
- パーツ語彙: `prompts.mjs` の `BASE_KIT_PARTS`（**`palette` キーが既に存在** — Phase 2 で使う）

**分かりにくさの仮説（コードレビューより。Phase 1 開始時に 🤖S の初見テストで検証してから着手すること）**:
1. ルートA/B が横並びで同時表示され、**どちらを・いつ使うべきか** の説明がない
2. ボタンを押した後 **何が起きたのか見えない**（リクエストは Queue タブに行くが、その場では小さな行が出るだけ。処理は外部エージェント任せという製品の核心が初見では理解不能）
3. ルートB の結果フロー（解析依頼 → 完了 → kitResults 一覧 →「パーツを選ぶ」→ プレビュー → 取り込み）が **画面のあちこちに散在**
4. ソース選択（手順1）は「採用済み画像」が前提だが、無い場合の空状態から **次の行動への導線が弱い**
5. ルートA のプロンプトが巨大な textarea で初見に威圧的

#### 実装ステップ

| # | タスク | 実施者 | テスト | 完了条件 | 状態 |
|---|--------|--------|--------|---------|------|
| 1.1 | **着手前の初見テスト**: 🤖S に「Create kit タブでキャラシートを1枚作れ。リポジトリ知識なし・画面の文言だけが頼り」を実施させ、詰まりポイントを記録（→仮説1-5の裏取り。これが Phase 1 の受け入れ基準のベースラインになる） | 🤖S | 詰まりポイント一覧が agent-logs に保存される | ⏳ |
| 1.2 | ルート選択をステップ化: 手順2を「目的で選ぶ」2枚の大きな選択カード（A: 参照画像からシートを作る / B: 1枚の画像をパーツ参照に分解する）にし、選択後にそのルートのフォームだけを表示（戻るリンク付き）。「いつどちらを使うか」の1行説明と所要ステップ数を各カードに記載 | 🤖 | 仮説1・5の解消 | ⏳ |
| 1.3 | 投入後のフィードバック強化: キュー投入直後に (a) fly-to-queue アニメ（既存実装を流用）、(b)「リクエストはキューに入りました。処理は `npm run demo-agent`（お試し）または通常のエージェントが行います」というトースト＋Queue タブへのリンク、(c) ルート画面内の pending 行に **処理方法のヒント** を表示 | 🤖 | 仮説2の解消。livePoll の結果到着演出と二重通知にならないこと | ⏳ |
| 1.4 | ルートB の結果フローを1画面に集約: 解析依頼 → 待機表示（livePoll 連動）→ 結果到着 → パーツ選択 → 取り込みまでを同一カード内のステップ表示にする。`kitResults` の既存データ構造・API は変えない | 🤖 | 仮説3の解消 | ⏳ |
| 1.5 | 空状態の導線: 採用済み画像が無いとき「Base タブで候補を採用する」ボタン＋ミニ図解（既存の空状態CTA様式を踏襲） | 🤖 | 仮説4の解消 | ⏳ |
| 1.6 | 文言の全面見直し: 専門語（採用/正準/キュー）に初出時ツールチップ or 補足文。**EN/JA 両マップ更新・パリティ確認** | 🤖 | i18n パリティ一致 | ⏳ |
| 1.7 | **事後の初見テスト（再走）**: 1.1 と同一指示を **別の新規 🤖S** に実施。1.1 の詰まりポイントが解消されたか比較レポート | 🤖S | 1.1 で出た詰まりの 8 割以上が解消、新たな重大詰まりゼロ | ⏳ |

#### Phase 1 E2E チェックリスト（🤖S が実施）

```
⏳ E2E-P1-01: ルートA完走 — サンプルデッキ(Aoi)で Create kit → ソース2枚選択 → ルートA →
   シート名変更 → キュー投入 → Queue タブに requested 行 → npm run demo-agent →
   結果到着トースト → 候補として登録されている（API /api/state で assets 増を確認）
⏳ E2E-P1-02: ルートB完走 — 解析依頼 → demo-agent で parts 完了 → 結果から3パーツ選択 →
   取り込み → Base タブに該当カテゴリのエントリが増えている
⏳ E2E-P1-03: 空状態 — 新規キャラ（採用ゼロ）で Create kit を開く → CTA から Base タブへ
   遷移できる
⏳ E2E-P1-04: デグレ確認 — タブ切替アニメ・Cmd+K・ツアー・Undo削除・比較スライダーが
   Create kit 改修後も動作（手動またはスクリプトで各1回）
⏳ E2E-P1-05: EN/JA 切替で全新規文言が翻訳されている（言語切替して kit タブを目視）
```

---

## Phase 2: カラーパレット統合 ⏳

**目標**: 「シートを作る」（ルートA）にカラーパレットを組み込み、生成されるシートの色再現品質を上げる。

**設計方針（実装前に必ず現状確認）**:
- `BASE_KIT_PARTS` には既に `palette` パーツ（カラースウォッチグリッド）が存在し、ルートBで
  パレット参照画像を作れる。**「パレット参照を持っているならシート生成時に自動同梱し、
  プロンプトで色の正(authority)として扱わせる」** のが本フェーズの核心。
- 生成サービス非依存を維持: パレットは **参照画像＋プロンプト文言** としてリクエストに載せるだけ。
  本体で色抽出などの画像処理はしない（依存ゼロ維持。`placeholder-art.mjs` 流用の軽量実装なら可）。

#### 実装ステップ

| # | タスク | 実施者 | テスト | 完了条件 | 状態 |
|---|--------|--------|--------|---------|------|
| 2.1 | ルートA に「カラーパレット」セクション追加: パレット判定は **① `entry.partKey === "palette"` → ② レガシー救済の id 接頭辞 `base-kit-palette` 一致** の2段（Q&A Q2 の決定。前提としてルートB取り込み時に全パーツへ `partKey` を永続化する）。該当エントリが **採用済みであれば** サムネイル表示＋「シートに同梱」トグル（既定ON） | 🤖 | パレット採用済みキャラでトグルが出る。partKey 永続化が normalizeState で消えないテストあり | ⏳ |
| 2.2 | パレット未保有時の導線: 「先にパレットを作る」ボタン → **palette 専用の `generate` リクエストを1クリック投入**（Q&A Q3 の決定。BASE_KIT_PARTS の palette hint からプロンプト自動組み立て、採用済み参照（master/顔優先）を `inputs.refImages` に添付）。**採用済み参照ゼロのキャラではボタンを出さず** Base タブ誘導（1.5 の空状態CTA）。補足文で「シート画像があるならルートBでも作れる」と1行案内 | 🤖 | 未保有キャラで1クリック投入できる。採用ゼロのキャラではボタン非表示 | ⏳ |
| 2.3 | シート生成リクエストへの反映: 同梱ONのとき (a) パレット画像を `inputs.refImages` に追加、(b) シートプロンプトに「添付のカラーパレットを色の正とする。髪・瞳・肌・衣装の色はパレットのスウォッチに厳密一致させ、HEX を読み取って転記する」相当の指示ブロックを自動付加（`BUILTIN_SHEET_TEMPLATE` 本文は直接書き換えず、付加ブロックとして合成 — ユーザーのカスタムテンプレにも同様に付く） | 🤖 | request JSON に palette の refImage とプロンプトブロックが入る（`--dry-run` か request ファイル目視で確認） | ⏳ |
| 2.4 | 品質ゲート連携: 品質ゲート有効のシート生成では、チェックプロンプト（`composeQualityCheckPrompt`）の比較対象にパレットも含める（パレット同梱時のみ）。既存の品質ゲート仕様（compare-if-visible）を踏襲 | 🤖 | 既存品質ゲートのテストがパスしたまま、パレット入りケースのテストが追加される | ⏳ |
| 2.5 | ユニットテスト: リクエスト組み立て（2.3）の payload 形状を server.test.mjs に追加（contract: 既存フィールドの意味は不変、追加のみ） | 🤖 | npm test 全パス | ⏳ |
| 2.6 | 🤖S 検証: 受け入れ条件のみ渡して「パレット採用済み/未保有の両キャラでルートAを実行し、生成された request JSON が条件を満たすか」を判定させる | 🤖S | 合格レポート | ⏳ |

#### Phase 2 E2E チェックリスト

```
⏳ E2E-P2-01: パレット作成 → 採用 → ルートAで同梱ON → キュー投入 → request JSON に
   palette 画像パスと色指示ブロックが含まれる
⏳ E2E-P2-02: 同梱OFFにすると request JSON から両方消える
⏳ E2E-P2-03: パレット未保有 → 「先にパレットを作る」→ palette 生成リクエストが
   キューに入る → demo-agent で完了 → 採用 → ルートAに戻るとトグルが出現
⏳ E2E-P2-04: 品質ゲートONのシート生成で、チェック対象にパレットが含まれる
```

---

## Phase 3: 初心者テスト＆磨き込み ⏳

**目標**: 「コンテキストなしの初心者が触る」前提のテストを体系的に実施し、出た詰まりを潰し切る。

**初心者テストのプロトコル（🤖S 用・このまま指示文として使う）**:

> あなたは AI 画像生成（ChatGPT や Midjourney）は日常的に使うが、CLI と Git に不慣れな
> イラスト制作者です。このリポジトリの知識は一切ありません。
> 渡されるのは GitHub の README（および画面に表示される文言）だけです。
> 次のゴールを **15分以内** に達成してください。途中でソースコードを読むことは禁止です。
> 1. ツールをインストールして起動する
> 2. サンプルキャラで「キャラシート」を1枚キューに入れる
> 3. お試しエージェントで結果を受け取り、候補を採用する
> 4. 採用した画像をギャラリーで確認する
>
> **行動ログを必ず残すこと**: 各ステップで「何を見て」「何を期待して」「何をして」「何が起きたか」。
> 詰まったら、何の情報があれば詰まらなかったかを記録。ゴール未達でも15分で終了し報告する。

#### 実装ステップ

| # | タスク | 実施者 | 完了条件 | 状態 |
|---|--------|--------|---------|------|
| 3.1 | 初心者テスト第1走（上記プロトコル、🤖S を新規コンテキストで起動） | 🤖S | 行動ログ保存 | ⏳ |
| 3.2 | ペルソナ違いで第2走: 「コーディングエージェント（Codex/Claude Code）にキュー処理を任せたい開発者」ペルソナで、AGENTS.md だけを渡して実処理導線を評価 | 🤖S | 行動ログ保存 | ⏳ |
| 3.3 | 詰まりの分類と修正: 2走分のログを「ドキュメント不足 / UI文言 / UI構造 / バグ」に分類し、各分類の上位を修正。**修正のたびに通常の品質コマンド一式** | 🤖 | 全分類の P0/P1 解消 | ⏳ |
| 3.4 | 再走: 3.1 と同一プロトコルを **別の新規 🤖S** で実施 | 🤖S | 15分以内にゴール4まで到達 | ⏳ |
| 3.5 | 👤最終確認: 人間が実機で同じ4ゴールを実施（本物の初見感覚の最終チェック）。スクリーンショットを agent-logs に保存 | 👤 | 👤が「迷わなかった」と判定 | ⏳ |

#### Phase 3 完了条件

- [ ] 🤖S 初心者テスト: 15分以内にゴール4まで到達（再現2回）
- [ ] 👤 実機確認済み
- [ ] 詰まりログと修正の対応表が agent-logs に保存されている

---

## Phase 4: マーケティング実施 ⏳

**目標**: X(Twitter) / note / 海外ブログへのローンチ投稿。**ドラフトは 🤖、投稿は必ず 👤**（公開行為のため）。

> **重要**: いかなる投稿・予約投稿も 🤖 が直接実行してはならない。🤖 はドラフトとチェックリスト
> 作成まで。アカウント認証情報を扱う作業もすべて 👤。

#### 事前に揃える素材（依存関係）

| 素材 | 状態 | 担当 |
|------|------|------|
| デモGIF（README の `docs/assets/readme/demo.gif` 差し込み枠） | ⏳ 👤が録画（絵コンテ: `docs/marketing/video-shotlist.md` の45秒・9ショット） | 👤 |
| デモ動画（45秒、X/note 埋め込み用） | ⏳ 同上 | 👤 |
| LP | ✅ https://aiarranger.github.io/image-arranger/ | - |
| OGP カード | ✅ 新ブランドで再生成済み | - |

#### 実装ステップ

| # | タスク | 実施者 | 完了条件 | 状態 |
|---|--------|--------|---------|------|
| 4.1 | `docs/marketing/` を作成し、媒体別ドラフトを生成: (a) X スレッド JA（1投目140字以内のフック＋4-6投、動画添付位置指定）、(b) X スレッド EN、(c) note 記事 JA（2,000-3,000字: 課題→デモ→使い方→哲学「生成しない」→OSS への誘導）、(d) dev.to 記事 EN（Show HN 転用可能な構成）、(e) Reddit r/StableDiffusion 投稿文 EN（コミュニティ規範に合わせ宣伝色を抑える） | 🤖 | 各媒体の文字数・画像/動画スロット・リンク（UTM付き）が明記されたドラフト | ⏳ |
| 4.2 | 投稿チェックリスト作成: 媒体ごとの「投稿前確認（リンク到達・画像表示・OGPプレビュー）/ 投稿手順 / 投稿後確認（メトリクス記録欄）」 | 🤖 | checklist.md 完成 | ⏳ |
| 4.3 | 🤖S レビュー: ドラフトを新規コンテキストの 🤖S に渡し「読者として刺さるか・誇大表現や事実誤認がないか・実際のリポジトリと食い違う記述がないか」を照合（README/LP と突き合わせ） | 🤖S | 指摘ゼロまたは修正済み | ⏳ |
| 4.4 | 👤 投稿実施: チェックリストに従い X(JA→EN) → note → dev.to → Reddit の順で投稿。各投稿のURLとメトリクスを checklist に記録 | 👤 | 全媒体投稿済み・URL記録 | ⏳ |
| 4.5 | 反応対応の準備: 想定問答集（「ComfyUIと何が違う？」「なぜ生成しない？」「Windows対応は？」等、README の Alternatives/OS matrix から生成） | 🤖 | FAQ ドラフト完成 | ⏳ |

#### Phase 4 完了条件

- [ ] 全媒体のドラフトが 🤖S レビュー済み
- [ ] 👤 による投稿完了・URL 記録
- [ ] 想定問答集が docs/marketing/ にある

---

## 進捗運用ルール

- タスク着手時に状態を 🚧、完了条件充足で ✅ に更新（Phase 見出しも連動）
- Phase は番号順に実施（Phase 0 の環境固定が全フェーズの前提）
- 各 Phase 完了時に `CHANGELOG.md` の Unreleased へ1行追加
- 受け入れ条件を満たせない場合は**勝手に条件を緩めず**、本ドキュメントに「ブロック理由」を追記して 👤 に確認

---

## Codex からの確認事項への回答（2026-06-13）

### Q1. 🤖S 検証は実際に別サブエージェントを起動するのか、「新規コンテキスト相当の検証ログ」でよいか

**仕組みは問わない。守るべき性質は「コンテキスト分離」**。優先順位:

1. サブエージェント機構が使えるなら実際に起動する（最優先）
2. 使えない場合は **別の新規 Codex セッション/実行** で代替してよい。ただし検証者に渡してよいのは
   (a) 受け入れ条件、(b) 本計画書記載のプロトコル文、(c) リポジトリへのアクセス、の3点のみ。
   実装時の会話・diff の説明・「こう直した」という情報を渡してはならない
3. **実装した本人（同一コンテキスト）が書いた「新規コンテキスト相当のログ」は不合格**。
   これは検証の意味（実装者バイアスの排除）を失うため

検証ログの先頭に「検証者に与えた入力一式」を必ず記録すること。これで分離が監査可能になる。

### Q2. palette 検出のための明示フィールド（`partKey` / `tags`）を追加してよいか

**追加してよい。`partKey` フィールドを推奨**（追加のみ・後方互換維持なので契約ルール違反にならない）:

- ルートB のパーツ取り込み時、**全パーツ**（palette に限らず）にエントリへ
  `partKey: "<BASE_KIT_PARTS の key>"` を永続化する。deck スキーマは未知フィールドを
  保持する設計（`normalizeState` で消えないことをテストで確認すること）
- `tags` での明示化は採用しない。tags はユーザーが編集・削除できる表示用データであり、
  機能判定の根拠には不向き（`"base-kit"` タグの既存用法は表示チップ用なので現状維持）
- **検出順序**: ① `entry.partKey === "palette"` → ② レガシー救済として id 接頭辞
  `base-kit-palette` 一致（既存デッキ向けフォールバック）。この2段で実装する
- request-spec.md の更新は不要（deck 内部のフィールドであり、リクエスト契約には載らない）

### Q3. 「先にパレットを作る」はルートB経由か、palette 専用の直接生成か

**palette 専用の直接生成リクエストを1クリック投入する（後者）を正とする**。理由:

- CTA の目的は「パレットを持っていない人を最短でパレット保有状態にする」こと。
  ルートB は分解元のシート画像が前提で、ステップも多く、目的に対して遠回り
- 実装: `BASE_KIT_PARTS` の palette エントリの hint からプロンプトを自動組み立てし、
  そのキャラの**採用済み参照**（master / 顔があれば優先）を `inputs.refImages` に添付して
  `generate` リクエストを投入（色の正は既存の採用済みアートから導く）
- **採用済み参照が1枚も無いキャラでは直接生成を出さない**。色の根拠が無いパレットは
  品質向上に寄与しないため、その場合は「先に Base タブで候補を採用する」導線
  （Phase 1 タスク 1.5 の空状態CTA）へ誘導する
- ルートB（シートから分解して palette を得る）は従来どおり並存。CTA の補足文で
  「手元にシート画像があるなら B ルートでも作れる」と1行案内する

### Q4. `round2-research.md` が見えない。デモ動画絵コンテはどうするか

**リポジトリ内に昇格済み: `docs/marketing/video-shotlist.md` を正とする**（本回答と同時にコミット）。

- 元ファイルは gitignore 配下の作業ログだったため、Phase 4 で必要な45秒・9ショットの
  絵コンテ部分を tracked ファイルとして移植した。当時指摘されていた撮影ブロッカー
  （G1 ライブ更新なし 等5件）は **すべて解消済み** である旨も追記してある
- README / LP / 既存 assets からの再構成・調整は **歓迎**。ただし shot 構成を変える場合は
  video-shotlist.md を更新してから Phase 4 のドラフトに反映すること（ドキュメントが正）
- 本計画書の frontmatter と Phase 4 素材表の参照先も video-shotlist.md に更新済み
