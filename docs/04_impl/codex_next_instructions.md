---
role: plan
depends_on:
  - docs/04_impl/codex_plan_kit_ux_env_marketing.md
  - docs/04_impl/codex_fixlist_2026-06-13.md
---
# Codex 次回作業指示書（2026-06-13 起票）

## 必読（毎セッション冒頭・省略禁止）

1. `workspace/_LOCAL_RULES.md` — このマシン固有のデータ安全ルール（正データ不可侵 /
   検証は /tmp＋4901-4999 / 後始末3点セット / **開発サーバー 4217 は作業後も必ず起動したまま**）
2. `AGENTS.md` の「Workspace & test-data safety」と「**Mandatory GUI/UX verification**」
   — フロントエンド変更は実画面検証（メイン＋コンテキストなしサブエージェントの二重確認）が必須
3. `docs/04_impl/codex_plan_kit_ux_env_marketing.md` の「品質担保の大原則」1〜9
   — 特に 9: **UIに内部メッセージを出さない・勝手に追加しない**

## 現状サマリ（2026-06-13 時点）

- Phase 0〜5・fixlist 21項目・Phase 6R（素材作成再設計＋スクロール根治＋内部文言一掃）まで実装済み。
  スクロールの根因は **View Transitions のフリーズ**で、スクロール可能画面と素材作成では VT 無効化
  ＋ watchdog `skipTransition()` で対処済み
- ギャラリーの契約は「**画像エントリの採用のみ・ベース対象外**」で確定（変更はオーナー承認必須）
- 品質ゲート（自動チェック）は**画像生成時専用**。素材作成には存在しない
- テスト 38/38・doctor・i18n パリティ・CSP 全合格。開発サーバー: 4217（オーナー専用・常設）
- **未完のオーナー依存項目**: 6R のオーナー実機受け入れ / demo.gif 録画 / Phase 3.5 実機確認 / 投稿

## 作業指示（優先順）

### N1. 6R のオーナー受け入れ対応 🔝

- 4217 は静的ファイルをリクエスト毎にディスクから配信するため再起動不要（コミット済みの最新が
  既に出ている）。オーナーからの指摘が来たら**最優先で修正** → 修正のたびに大原則の必須コマンド
  ＋ GUI 二重検証（メイン＋コンテキストなし🤖S）を回す
- オーナー指摘が「実機でしか再現しない」場合: 多量エントリのダミーデッキを /tmp に生成して
  再現環境を作る（オーナーのデッキは使用禁止）。再現できない場合は再現手順をオーナーに確認

### N2. ドキュメント同期（6R でUIが変わったため必須）

- README.md / README.ja.md: Create kit の説明（旧ルートA/Bカード・パレット・品質ゲート言及が
  残っていれば現UIに合わせて全面更新）。glossary・スクリーンショット記述の整合も確認
- docs/marketing/: note-ja / x-ja / x-en / devto の本文・スクリーンショット指示が**旧kit UI を
  前提にしていないか**全件照合し更新。x-followup.md の「品質ゲート」表記は新ラベル
  「生成結果の自動チェック」に合わせる
- docs/marketing/video-shotlist.md: shot 2（Create kit 操作）と shot 6（品質ゲート→自動チェック表記）
  を新UIの実操作に合わせ更新
- CHANGELOG.md: 6R の変更（再設計 / VT スクロール修正 / パレットUI撤去 / 内部文言一掃 /
  ギャラリー契約）が Unreleased に過不足なく載っているか照合
- AGENTS.md / docs/request-spec.md: 「シート生成に qualityGate は付かない」が読み取れるか確認
- 各ドキュメント変更後: リンク切れチェックと EN/JA パリティ

### N2.5 軽微UI（QA指摘 2026-06-13）

- キュータブの行に依頼ID（`req_20260613..._xxxx`）が生表示される。一般ユーザーには無意味な
  文字列なので、既定表示からは外す（詳細表示やツールチップへ格下げ。Queue の操作・
  トラブル時の特定用途は維持）

### N3. テストの追補

- server.test.mjs: シート生成リクエストに `qualityGate` とパレットブロックが**付かない**ことの
  恒久テストを追加（6R のデグレ防止）。アップロード→マスター登録経路のペイロードテストも追加
- CI グリーン確認（boot smoke は画像リクエスト処理込みであること）

### N4. リリース準備（実施はオーナー合図待ち・準備だけ先行）

- `npm pack --dry-run` で同梱物確認（files allowlist に漏れ・余計が無いか）
- CHANGELOG から GitHub Release ノート草案を `docs/marketing/release-notes-draft.md` に生成
- バージョン提案（現 0.1.0 → 0.2.0 を提案。判断はオーナー）

### N5. 提案済み・未着手の候補（オーナー承認が出たら着手）

- Queue 履歴＋統計ストリップ（R2 バックログ#8、効果3/5）
- LP に新機能（Cmd+K / 自動チェック / PNGメタデータ取り込み）の bento セル追加
- いずれも**勝手に着手しない**。本指示書にオーナーの承認が追記されてから

## 完了報告の形式

各 N 項目ごとに: 変更ファイル / 実行した検証コマンドと結果 / GUI 二重検証の証跡
（コンテキストなし🤖S のレポート要約）/ 残課題。実装ログは `agent-logs/<日付>-codex-next/` へ。

## 担当分担（再掲）

| 誰が | 何を |
|------|------|
| **Codex** | N1〜N4（N5は承認後）。修正のたび大原則の必須コマンド＋GUI二重検証 |
| **Claude Code** | Codex 完了後の独立検証・実ブラウザ最終リグレッション・レビュー・コミット |
| **👤** | 6R 実機受け入れ / demo.gif 録画（絵コンテ: docs/marketing/video-shotlist.md）/ リリース・投稿の最終GO |
